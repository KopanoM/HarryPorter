<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="qubit.engineering.harryporter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.ShowAllSpells"></activity>
        <activity android:name=".UI.ShowMembers" />
        <activity android:name=".UI.HousesList" />
        <activity android:name=".UI.Spells" />
        <activity android:name=".UI.ShowMember" />
        <activity android:name=".UI.IndividualHouses" />
        <activity android:name=".UI.Houses" />
        <activity android:name=".UI.IntroductionScreens">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.MainActivity" />
    </application>

</manifest>